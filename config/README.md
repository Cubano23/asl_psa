# PSA

## Initialisation de la configuration

- Se rendre dans le répertoire asl_psa/Config/

```bash
// Pour précharger la configuration local
bash config.sh local

// Pour précharger la configuration de preprod
bash config.sh preprod

// Pour précharger la configuration de prod
bash config.sh prod
```